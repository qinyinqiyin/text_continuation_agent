flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=21.2.0
dashscope>=1.19.3
python-dotenv>=1.0.0
# 以下依赖用于知识库功能（可选，如果内存不足可以先注释掉）
# 在Vercel Serverless环境中，可以暂时禁用知识库功能以避免构建时OOM
# torch>=2.0.0,<3.0.0
# transformers>=4.30.0,<5.0.0
# faiss-cpu>=1.7.4
# numpy>=1.24.0
# modelscope>=1.9.0


